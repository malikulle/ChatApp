@model List<Chat>

<div class="side-menu">
    @foreach (var chat in Model)
    {
        <a asp-controller="Chat" asp-action="Index" asp-route-id="@chat.Id" class="room-button">@chat.Name</a>
    }

    <a class="room-button" id="create-room-btn" href="#"><strong>+</strong></a>

</div>
